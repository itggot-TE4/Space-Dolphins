import { Module } from 'vuex'

export default {
  namespaced: true,
  state: {
    
  },
  actions: {
    async authorize({ commit, dispatch } : any , credentials : {}) {
      try {
        if () {
          
        }
      } catch (err) {
          
      }
    }
  }
}